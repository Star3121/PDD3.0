# 迁移脚本环境变量示例
# 使用方法：复制为 migration.env 并填入实际值，然后执行：
#   node -r dotenv/config scripts/migrate_to_new_supabase.js dotenv_config_path=scripts/migration.env

# 数据源选择：supabase 或 sqlite（默认 supabase）
OLD_DB_TYPE=supabase

# 如果是旧 Supabase 项目作为数据源：
OLD_SUPABASE_URL=https://old-project-id.supabase.co
OLD_SUPABASE_SERVICE_ROLE_KEY=old_service_role_key

# 如果是本地 sqlite 作为数据源：
LOCAL_SQLITE_PATH=./database.sqlite

# 目标新 Supabase 项目：
NEW_SUPABASE_URL=https://new-project-id.supabase.co
NEW_SUPABASE_SERVICE_ROLE_KEY=new_service_role